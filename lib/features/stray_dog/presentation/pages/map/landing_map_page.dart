import 'package:flutter/material.dart';

class LandingMap extends StatelessWidget {
  const LandingMap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("MAPA :D, segunda vista"),
    );
  }
}