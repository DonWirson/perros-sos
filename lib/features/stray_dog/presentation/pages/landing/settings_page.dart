import 'package:flutter/material.dart';

class Landingsettings extends StatelessWidget {
  const Landingsettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Test settings, vista 3"),
    );
  }
}
